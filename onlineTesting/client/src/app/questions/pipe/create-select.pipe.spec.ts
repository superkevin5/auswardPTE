import { CreateSelectPipe } from './create-select.pipe';

describe('CreateSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
